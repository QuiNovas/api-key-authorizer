version: 2
jobs:
  develop:
    branches:
      only:
        - develop
    docker:
      - image: circleci/python:2.7
    steps:
      - checkout
      - run: python setup.py ldist
  master:
    branches:
      only:
        - master
    docker:
      - image: circleci/python:2.7
    steps:
      - checkout
      - run: echo "Hello master"

